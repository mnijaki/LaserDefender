using UnityEngine;

// Readme.
public class Readme : MonoBehaviour
{

  // Only for purpose of README.
  // * Score, lifes, healt, weapon types should move to other levels, now thy reset evrey time scene change!
  // * Add flames on engine when ships are moving (particle system or static sprite)
  // * Add power up
  // * Add rocks
  // * Add antoher spacships
  // * Add Extra life
  // * Add Invincible shield
  // * Add destroing shield
  // * Add multiple weapons to change
  // * Add co - op
  // * Add colision between ships
  // * Add support for all info elements
  // * Add animations on Win and Lose scene
  // * Add characteristic from player ship to enemies ships
  // * Add levels
  // * Add more animations
  // * Animation of ship going to next lvl
  // * Header with info at the beggining of the level
  // * Ask what is best way to load next level or spawn new ship? (corutines or inoke repeating)
  // * Mayby waepon types should be loaded static in start? becouse double childing of prefabs is forbidden?
  // * Rotate projectiles
  // * Allow switching only to aviable weapons

} // End of Readme